# Ruler Configuration File
# See https://okigu.com/ruler for documentation.

# Default agents to run when --agents is not specified
default_agents = ["amp", "claude", "codex", "gemini-cli", "opencode", "qwen", "jules", "openhands"]

# Ruler automatically discovers all .md files from .ruler/ directory recursively
# The ruler:apply script copies README.md → .ruler/01-readme.md (01- prefix for ordering)
# Additional prompts are stored in .ruler/prompts/ and auto-discovered by Ruler

# --- Global MCP Server Configuration ---
[mcp]
# Enable/disable MCP propagation globally (default: true)
enabled = true
# Global merge strategy: 'merge' or 'overwrite' (default: 'merge')
merge_strategy = "merge"

# Exa Code MCP provides code search context via mcp-remote
[mcp_servers.exa]
command = "bunx"
args = ["mcp-remote", "https://mcp.exa.ai/mcp"]






# --- Global .gitignore Configuration ---
[gitignore]
# Enable/disable automatic .gitignore updates (default: true)
enabled = true