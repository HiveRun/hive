{
  "name": "@synthetic/db",
  "version": "0.1.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "test": "vitest",
    "test:run": "vitest run",
    "check": "biome check --write .",
    "check-types": "tsc --noEmit",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:generate": "drizzle-kit generate"
  },
  "dependencies": {
    "@synthetic/config": "workspace:*",
    "drizzle-orm": "^0.44.2"
  },
  "devDependencies": {
    "@types/bun": "^1.2.6",
    "@types/better-sqlite3": "^7.6.12",
    "better-sqlite3": "^11.8.1",
    "drizzle-kit": "^0.31.2",
    "tsdown": "catalog:",
    "typescript": "catalog:",
    "vitest": "^2.1.8"
  }
}
