{
  "name": "@hive/desktop-electron",
  "private": true,
  "version": "0.1.0",
  "description": "Hive desktop runtime shell powered by Electron.",
  "author": "HiveRun",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsdown src/main.ts src/preload.ts --format cjs --out-dir dist --external electron",
    "prepare:renderer": "bun run scripts/prepare-renderer.ts",
    "start": "bun run build && electron dist/main.js",
    "package": "bun run build && bun run prepare:renderer && electron-builder --config electron-builder.yml --publish never"
  },
  "devDependencies": {
    "electron": "^37.2.3",
    "electron-builder": "^26.0.12",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
