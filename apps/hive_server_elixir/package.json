{
  "name": "@hive/server-elixir",
  "private": true,
  "scripts": {
    "dev": "bash -lc 'mise x -C . -- mix ecto.migrate && exec mise x -C . -- iex -S mix phx.server'",
    "test:run": "mise x -C . -- mix test",
    "check": "mise x -C . -- mix format --check-formatted && mise x -C . -- mix test"
  }
}
