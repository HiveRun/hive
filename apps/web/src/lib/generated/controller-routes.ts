// This file is auto-generated by AshTypescript. Do not edit manually.


export type * from "./ash_types";
/**
 * Configuration options for typed controller requests
 */
export interface TypedControllerConfig {
  headers?: Record<string, string>;
  fetchOptions?: RequestInit;
  customFetch?: (
    input: RequestInfo | URL,
    init?: RequestInit,
  ) => Promise<Response>;
}

/**
 * Internal helper function for making typed controller requests
 */
export async function executeTypedControllerRequest(
  url: string,
  method: string,
  _actionName: string,
  body: string | undefined,
  config?: TypedControllerConfig,
): Promise<Response> {
    const processedConfig = config || {};

  const headers: Record<string, string> = {
    "Content-Type": "application/json",
    ...processedConfig.headers,
  };

  const fetchFunction = processedConfig.customFetch || fetch;
  const fetchInit: RequestInit = {
    ...processedConfig.fetchOptions,
    method,
    headers,
    ...(body !== undefined ? { body } : {}),
  };

  const response = await fetchFunction(url, fetchInit);


  return response;
}

/**
 * Path helper for /api/agents/models
 */
export function agentModelsPath(query?: { workspaceId?: string }): string {
  const base = "/api/agents/models";
  const searchParams = new URLSearchParams();
  if (query?.workspaceId !== undefined) searchParams.set("workspaceId", String(query.workspaceId));
  const qs = searchParams.toString();
  return qs ? `${base}?${qs}` : base;
}

/**
 * Path helper for /api/agents/sessions/:id/messages
 */
export function agentSessionMessagesPath(path: { id: string }): string {
  return `/api/agents/sessions/${path.id}/messages`;
}

/**
 * Path helper for /api/agents/sessions/:id/models
 */
export function agentSessionModelsPath(path: { id: string }): string {
  return `/api/agents/sessions/${path.id}/models`;
}

/**
 * Path helper for /api/cells/:id/diff
 */
export function cellDiffPath(path: { id: string }, query?: { mode?: string; summary?: string; files?: string }): string {
  const base = `/api/cells/${path.id}/diff`;
  const searchParams = new URLSearchParams();
  if (query?.mode !== undefined) searchParams.set("mode", String(query.mode));
  if (query?.summary !== undefined) searchParams.set("summary", String(query.summary));
  if (query?.files !== undefined) searchParams.set("files", String(query.files));
  const qs = searchParams.toString();
  return qs ? `${base}?${qs}` : base;
}

/**
 * Path helper for /api/cells/:id/resources
 */
export function cellResourcesPath(path: { id: string }, query?: { includeHistory?: boolean; includeAverages?: boolean; includeRollups?: boolean; historyLimit?: number; rollupLimit?: number }): string {
  const base = `/api/cells/${path.id}/resources`;
  const searchParams = new URLSearchParams();
  if (query?.includeHistory !== undefined) searchParams.set("includeHistory", String(query.includeHistory));
  if (query?.includeAverages !== undefined) searchParams.set("includeAverages", String(query.includeAverages));
  if (query?.includeRollups !== undefined) searchParams.set("includeRollups", String(query.includeRollups));
  if (query?.historyLimit !== undefined) searchParams.set("historyLimit", String(query.historyLimit));
  if (query?.rollupLimit !== undefined) searchParams.set("rollupLimit", String(query.rollupLimit));
  const qs = searchParams.toString();
  return qs ? `${base}?${qs}` : base;
}

/**
 * Path helper for /api/templates
 */
export function listTemplatesPath(query?: { workspaceId?: string }): string {
  const base = "/api/templates";
  const searchParams = new URLSearchParams();
  if (query?.workspaceId !== undefined) searchParams.set("workspaceId", String(query.workspaceId));
  const qs = searchParams.toString();
  return qs ? `${base}?${qs}` : base;
}

/**
 * Path helper for /api/templates/:id
 */
export function showTemplatePath(path: { id: string }, query?: { workspaceId?: string }): string {
  const base = `/api/templates/${path.id}`;
  const searchParams = new URLSearchParams();
  if (query?.workspaceId !== undefined) searchParams.set("workspaceId", String(query.workspaceId));
  const qs = searchParams.toString();
  return qs ? `${base}?${qs}` : base;
}

/**
 * Path helper for /api/workspaces/browse
 */
export function workspaceBrowsePath(query?: { path?: string; filter?: string }): string {
  const base = "/api/workspaces/browse";
  const searchParams = new URLSearchParams();
  if (query?.path !== undefined) searchParams.set("path", String(query.path));
  if (query?.filter !== undefined) searchParams.set("filter", String(query.filter));
  const qs = searchParams.toString();
  return qs ? `${base}?${qs}` : base;
}
